# 추가 기능 백로그

이미 GROWTH.md(구독자 확보·리텐션)와 MONETIZATION.md(유료 기능)에 정리된
것과 겹치지 않는 "더 넣을 수 있는 기능"만 모았다.

## ✅ 구현되어 실제 데이터로 테스트 끝남 (2026-08-27)

| 기능 | 파일 | 내용 |
| --- | --- | --- |
| 마감일 캘린더(.ics) | `docs/deadlines.ics` | 구글/애플 캘린더에 "구독"하면 마감일이 자동으로 뜬다 |
| RSS 피드 | `docs/feed.xml` | 이메일 없이도 RSS 리더로 구독 가능 |
| 상시 웹 아카이브 | `docs/archive/*.html` | 매일 쌓여 검색 유입 자산이 됨 |
| **임베드 위젯** | `docs/widget.html` | `<iframe src=".../widget.html">`로 다른 블로그/사이트에 TOP3를 그대로 심을 수 있음. salary-calculator에도 바로 쓸 수 있다 |
| **아카이브 검색** | `docs/archive/index.html` + `docs/search-index.json` | 서버 없이 브라우저 JS만으로 지난 공고를 키워드로 검색. 매일 `build_digest.py`가 인덱스에 오늘자를 누적한다 |
| **디스코드/슬랙 웹훅 중계** | `automation/notify_webhooks.py` | `DISCORD_WEBHOOK_URL`/`SLACK_WEBHOOK_URL` 시크릿만 넣으면 발행 즉시 커뮤니티 채널에도 자동 전송. 완전 무료 |
| **개인화(유료 전용) 필터링** | `automation/send_email_brevo.py`의 `personalize_html` | `subscribers.csv`에 `tier=paid`이고 `region`/`tags`를 채운 구독자만 그 조건에 맞는 공고만 골라 받는다. `tier=free`는 항상 전체 다이제스트 — MONETIZATION.md의 "맞춤 필터는 유료 전용" 설계를 코드로 그대로 구현한 것 |
| **국내 체류 외국인 창업가용 영문 섹션** | `common.is_english_title` → `build_digest.render_html`의 "🌐" 섹션 | 번역 없이도 K-Startup 원문 중 한글이 전혀 없는 공고(실제로 "Startup Korea Special Visa" 등 존재 확인됨)를 따로 모아 보여줌 |
| **브라우저 푸시(OneSignal) 연동 코드** | `build_digest.py`의 `_onesignal_snippet`, `automation/notify_onesignal.py` | `ONESIGNAL_APP_ID`/`ONESIGNAL_API_KEY`를 설정하면 자동으로 켜짐. 코드는 완성했지만 **OneSignal 자체 가입/무료한도는 아직 실계정으로 검증 못 함** — 아래 "도입 전 재확인" 참고 |

## ⚠️ 의도적으로 넣지 않은 것 (검증 결과 "무료"가 아니었음)

- **카카오톡 알림톡/브랜드메시지(구 친구톡)**: 2026년 기준 알림톡은 건당
  8원~, 브랜드메시지는 친구 23~25원/비친구 33~35원으로 **완전 유료**임을
  확인했다(무료 발송 불가). "개발비 0원" 원칙과 정면으로 어긋나서 자동화
  파이프라인에는 넣지 않았다. 카카오톡 채널 자체는 여전히 무료로 만들
  수 있으니, GROWTH.md에 있는 대로 **오픈채팅방에 사람이 직접 공유하는
  방식**까지만 쓴다. 유료 전환 후 매출이 생기면 그때 알림톡 도입을
  다시 검토할 것(원가 대비 이득이 있는지는 그 시점에 재계산).

## 🔜 도입 전 반드시 재확인해야 하는 것

- **OneSignal 무료 플랜**: "구독자 3만 명까지 무료"라는 정보를 여러
  리뷰에서 일관되게 확인했지만, 신용카드 요구 여부·발송 건수 제한까지는
  이번에 실계정으로 확인하지 못했다. 실제로 쓰려면 onesignal.com에서
  직접 가입해 무료 한도를 화면으로 재확인한 뒤 `ONESIGNAL_APP_ID`/
  `ONESIGNAL_API_KEY`를 GitHub Secrets에 등록할 것(MY_SETUP_CHECKLIST.md 참고).

## 💡 아직 아이디어 단계 (다음 후보)

| 기능 | 내용 | 비고 |
| --- | --- | --- |
| **다국어(영문) 전체 번역** | 국문 공고까지 영문으로 번역해서 완전한 영문판 발행 | 무료 번역 API는 대부분 글자수 제한이 있어(예: DeepL 무료 월 50만자) 전체 자동 번역엔 부족할 수 있음. 지금은 "원문이 이미 영문인 것만 모으는" 수준(위 구현 완료분)으로 충분히 가치 있음 |
| **Brevo 연락처 API로 구독자 관리 이전** | 지금은 구독자 명단을 GitHub Secret(base64 CSV)으로 넣는 방식이라 사람이 갱신해야 함 | 구독자가 많아지면 Brevo의 무료 연락처 관리 기능으로 옮기고, 발송 스크립트가 Brevo Contacts API로 명단을 직접 읽어오게 바꾸는 게 낫다. 구독자 300명 넘어 유료 전환을 검토하는 시점과 맞물려서 같이 할 것 |

## 도입 판단 기준

새 기능을 넣기 전에 항상 이 순서로 검토한다.

1. **개발비 0원 원칙에 맞는가** — 무료 티어가 실제로 존재하는지 먼저
   확인한다(카카오톡 알림톡처럼 "무료인 줄 알았던 게 아니었던" 경우가
   실제로 있었다)
2. **매일 다시 오게 만드는가, 아니면 그냥 부가기능인가** — GROWTH.md의
   리텐션 철학과 맞는지
3. **1인 운영으로 유지 가능한가** — 매일 손이 더 가는 기능이면 자동화
   없이는 넣지 않는다
